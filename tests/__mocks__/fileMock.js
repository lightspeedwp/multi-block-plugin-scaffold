/**
 * File Mock for Jest
 *
 * Mocks file imports (images, fonts, etc.) in Jest tests
 * Returns a simple string path instead of actual file content
 *
 * @package
 */

module.exports = 'test-file-stub';
